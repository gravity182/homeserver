{
  "extends": [ "config:base" ],
  "baseBranches": [ "master" ],
  "autodiscover": false,
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "logLevel": "debug",
  "helm-values": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Docker image updates",
      "matchDatasources": "docker",
      "matchManagers": [ "helm-values" ],
      "major": { "enabled": false },
      "minor": { "automerge": false },
      "patch": { "automerge": true, "labels": [ "renovate", "patch-update" ] },
      "commitMessageSuffix": "{{#if major}} ⚠️{{else if minor}} ✨{{else}} 🩹{{/if}}",
      "branchTopic": "renovate/{{datasource}}-{{depName}}-{{currentVersion}}-to-{{newVersion}}-{{chartName}}",
      "prHeader": "This PR updates an image for the `{{chartName}}` Helm chart.",
      "prFooter": "If you find this update useful, please consider [sponsoring Renovate](https://renovatebot.com/donate)."
    }
  ]
}
