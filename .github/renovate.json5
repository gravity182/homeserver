{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [ "config:recommended" ],
  "baseBranches": [ "master" ],
  "branchPrefix": "renovate/",
  "repositories": [
    "gravity182/homeserver"
  ],
  "prConcurrentLimit": 10,
  "helm-values": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Docker image updates",
      "matchDatasources": "docker",
      "matchManagers": [ "helm-values" ],
      "labels": [ "renovate" ],
      "major": { "automerge": false, "labels": [ "major-update" ] },
      "minor": { "automerge": false, "labels": [ "minor-update" ] },
      "patch": { "automerge": true, "labels": [ "patch-update" ] },
      "commitMessageSuffix": "{{#if isMajor}} ⚠️{{else if isMinor}} ✨{{else}} 🩹{{/if}}"
    }
  ]
}
