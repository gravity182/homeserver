{{- if .Values.services.calibre.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.calibre.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.services.calibre.name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.calibre.ports.http }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ .Values.services.calibre.name }}
{{- end }}
