{{- if .Values.services.calibre.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.calibre.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.services.calibre.name }}
spec:
  revisionHistoryLimit: 3
  replicas: {{ .Values.services.calibre.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.services.calibre.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.services.calibre.name }}
    spec:
      initContainers:
      {{- if .Values.services.calibre.vpn.enabled }}
        - name: gluetun
          image: ghcr.io/qdm12/gluetun:v3.40
          imagePullPolicy: IfNotPresent
          restartPolicy: Always
          securityContext:
            capabilities:
              add: ["NET_ADMIN"]
          env:
            - name: TZ
              value: {{ required "A valid timezone required!" .Values.host.tz | quote }}
            - name: VPN_SERVICE_PROVIDER
              value: custom
            - name: VPN_TYPE
              value: wireguard
            - name: HTTP_CONTROL_SERVER_ADDRESS
              value: ":8001"
            - name: DNS_KEEP_NAMESERVER
              value: "on"
            - name: FIREWALL_OUTBOUND_SUBNETS
              value: "10.42.0.0/15"
          volumeMounts:
            - name: wg0-conf
              mountPath: /gluetun/wireguard/
              readOnly: true
            - name: dev-net-tun
              mountPath: /dev/net/tun
        - name: verify-vpn
          image: busybox
          command:
            - sh
            - -c
            - |
              while ! ping -c 1 8.8.8.8; do
                echo "Ping failed, retrying in 5"
                sleep 5
              done
              echo "VPN connection established successfully, exiting"
      {{- end }}
      containers:
        - name: calibre
          image: "{{ .Values.services.calibre.image.repository }}:{{ .Values.services.calibre.image.tag }}"
          imagePullPolicy: {{ .Values.services.calibre.image.pullPolicy }}
          env:
            {{- range $key, $value := .Values.services.calibre.env }}
            - name: {{ $key | quote }}
              value: {{ $value | quote }}
            {{- end }}
            - name: PUID
              value: {{ required "A valid UID required!" .Values.host.uid | quote }}
            - name: PGID
              value: {{ required "A valid GID required!" .Values.host.gid | quote }}
            - name: TZ
              value: {{ required "A valid timezone required!" .Values.host.tz | quote }}
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /config
            - name: ingest
              mountPath: /cwa-book-ingest
            - name: data
              mountPath: /calibre-library
            - name: gmail-credentials
              mountPath: /app/calibre-web/gmail.json
              subPath: gmail.json
              readOnly: true
      volumes:
        {{- if .Values.services.calibre.vpn.enabled }}
        - name: wg0-conf
          secret:
            secretName: gluetun-wireguard-conf
            optional: false
        - name: dev-net-tun
          hostPath:
            path: /dev/net/tun
            type: CharDevice
        {{- end }}
        - name: config
          hostPath:
            path: "{{ .Values.services.calibre.config }}"
            type: Directory
        - name: ingest
          hostPath:
            path: "{{ .Values.services.calibre.ingest }}"
            type: Directory
        - name: data
          hostPath:
            path: "{{ .Values.services.calibre.data }}"
            type: Directory
        - name: gmail-credentials
          secret:
            secretName: calibre-gmail-credentials
            items:
              - key: gmail.json
                path: gmail.json
            optional: true
{{- end }}
