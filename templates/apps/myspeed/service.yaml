{{- if .Values.services.myspeed.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.myspeed.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.services.myspeed.name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.myspeed.ports.http }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ .Values.services.myspeed.name }}
{{- end }}
